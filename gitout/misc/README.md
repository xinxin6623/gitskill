# misc · 默认 domain

未指定 `--domain` 时，所有 entry 落在这里。

## 健康度面板

```yaml
domain: misc
active_entries: 35 / 30  # ⚠️ 超出上限，需 prune
snippets: 0 / 50
last_pruned: never
last_scan: 2026-05-22 (5 themes × 5 projects, batch scan)
health: red  # 超容量上限
导航页: entries/INDEX.md
```

> 状态由 `/gitout prune` 更新。green / yellow / red 规则见上级 `../README.md`。
